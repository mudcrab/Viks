{
	mainClass: {
		setup(),
		draw(),
		update()
	},
	stage: {
		getDrawables -> updatePositions -> renderEntities
	},
	core: {
		initStuff,
		renderStageObjects
	}
}
// layering

addChild(entity, layer)
/*
render() ->
	getLayers()
		iterate over every layerin
			check if something needs drawing
				add to drawables with layer indication
				render layer by layer
*/

//
// render is for rendering shit, update is for updating positions, catching keys, etc